## RoadMap
- C/C++ IO模板

  有些网络笔试不是 LeetCode 的形式，需要手动输入输出——传统 ACM 的方式，特此记录

- STL 容器
  
  需要说明的是，很多方法在容器中的接口是一致的，所以并没有全部测试一遍；
  有些方法则会因为容器本身的性质被屏蔽——比如 queue 就只有 push 方法，而不区分 push_back 还是 push_front

  - 向量 vector
  - 字典 map
  - 集合 set
  - 链表 list
  - 栈 stack
  - 队列、双端队列
  - 堆、优先队列